# Copy this file to .env and fill in real values before running docker compose.
# .env is gitignored — never commit real secrets.

# Generate with:  mix phx.gen.secret
SECRET_KEY_BASE=

# Public hostname the app is served from (used for URLs / force_ssl exclusion).
PHX_HOST=localhost

# How the site is reached publicly — drives URL generation in magic-link emails.
# A non-default port (not 80 for http, not 443 for https) is included in links.
# Local compose (plain HTTP on :4000):
PHX_SCHEME=http
PHX_PORT=4000
# Behind a TLS terminator, use instead:
#   PHX_SCHEME=https
#   PHX_PORT=443

# Database credentials for the TimescaleDB container.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=dtu_app_prod

# DB connection pool size.
POOL_SIZE=15

# Embedded MQTT broker.
MQTT_BROKER_ENABLED=true

# DNS alias shown to users as the MQTT broker host in the device setup modal.
# Set this when MQTT runs on a different domain than the web app (e.g. mqtt.example.com).
# Leave empty to use PHX_HOST (broker and web app on the same host).
MQTT_HOST=

# MQTTS endpoint (when the broker is fronted by a TLS terminator such as
# Traefik). Set MQTTS_HOST to the public DNS name DTUs should connect to
# over TLS, e.g. mqtt.example.com — the device setup modal will then show
# both mqtts://MQTTS_HOST:MQTTS_PORT (recommended) and the plain MQTT
# fallback so devices that don't support TLS (e.g. some AhoyDTU builds)
# can still connect on MQTT_HOST:1883.
# Leave MQTTS_HOST empty to keep the broker on plain MQTT (1883) only.
# The broker itself always listens plain TCP on MQTT_BROKER_PORT inside the
# container; TLS is terminated by the reverse proxy in front.
MQTTS_HOST=
MQTTS_PORT=8883

# Transactional email via the Resend API (https://resend.com).
# Leave RESEND_API_KEY empty to fall back to the in-memory mailer (emails are
# swallowed — magic-link login will not work without a real key).
RESEND_API_KEY=
# Sender address — must be on a domain you have verified in Resend.
# Use the "Name <address@domain>" form, e.g. "DtuApp <noreply@mail.example.com>".
MAIL_FROM=DtuApp <noreply@localhost>
